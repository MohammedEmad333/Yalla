server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # تطبيق صفحة واحدة (SPA): أي مسار غير موجود يُعاد توجيهه لـ index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
